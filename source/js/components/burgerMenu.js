class BurgerMenu {
  constructor(btnId) {
    this.btnBurger = document.getElementById(btnId);

    this._triggers();
  }

  _triggers() {
    this.btnBurger.addEventListener('click', () => {

    });
  }
}
